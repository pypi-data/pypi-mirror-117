from setuptools import setup

setup(name='urvog-distributions',
      version='0.11',
      description='Gaussian and Binomial distributions',
      packages=['urvog-distributions'],
      zip_safe=False)
