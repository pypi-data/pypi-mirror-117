"""
Setup dzy package
"""
from setuptools import setup, find_packages

setup(
    name="dzy",
    version="1.0.4",
    description="dzy Python package",
    author="dzy",
    packages=find_packages(),
    python_requires=">=3.6"
    # install_requires=[""]
)
