#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['six>=1.9']

extras_require = \
{":python_version < '3.0.0'": ['mutagen>=1.43'],
 ":python_version < '3.4.0'": ['enum34>=1.0.4'],
 ":python_version >= '3.0.0'": ['mutagen>=1.45'],
 'test': ['tox']}

setup(name='mediafile',
      version='0.7.0',
      description="Handles low-level interfacing for files' tags. Wraps Mutagen to",
      author='Adrian Sampson',
      author_email='adrian@radbox.org',
      url='https://github.com/beetbox/mediafile',
      py_modules=['mediafile'],
      install_requires=install_requires,
      extras_require=extras_require,
     )
