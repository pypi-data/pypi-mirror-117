[tool.poetry]
name = "transferhub"
version = "0.0.1"
description = ""
authors = ["JanSeidlein <jan@dmesh.io>"]

[tool.poetry.dependencies]
python = "^3.6.1"
requests = "^2.26.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
