[tool.poetry]
name = "gbif-blocking-occurrence-download"
packages = [
    { include = "./gbif_blocking_occurrences_download.py" }
]
version = "0.1.0"
description = ""
authors = ["Nicolas Noé <nicolas.noe@inbo.be>"]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.26.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
